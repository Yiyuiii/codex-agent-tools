namespace CodexAgentTools.HostAcceptance
{
    internal static class Program
    {
        private static int Main()
        {
            // The executable entry point is intentionally inert until the separately
            // reviewed Windows host/pipe layer owns all required observations.
            return 1;
        }
    }
}
