# codex-agent-tools 0.1.1-beta.1 发布审阅

日期：2026-07-30

状态：已由 GitHub Actions OIDC 发布到 npm `next`，公共 npm 隔离验收和官方
活动插件升级通过；等待完整宿主重启后的修复版真实宿主复验。

## 发布目的

`0.1.1-beta.0` 已修复插件相对 runtime 缺少工作目录的问题。维护者彻底退出并
重新打开 Codex 后，新建任务首次从版本化插件缓存发现了
`external_review` / `external_delegate`，同时保留旧
`cc_review` / `cc_delegate`，证明 `cwd: "."` 修复有效。

真实 Kimi K3 宿主 review 随后通过；Ark Coding Plan 宿主 review 在启动 Pi 前
以“缺少凭据”失败。只读进程环境核对只报告变量是否存在，不读取或记录值，确认
父 App 进程拥有 `API_KEY_DOUBAO_CODING`，但插件 MCP 没有收到它。

根因是 stdio MCP 配置没有声明 `env_vars`。Codex 官方配置参考把
`env_vars` 定义为从本地父环境转发给 stdio MCP 的变量白名单；`env` 才是静态值。
插件原 manifest 两者都没有，因此活动宿主按隔离边界没有转发 Ark 凭据。

## 改动范围

- 插件 `.mcp.json` 增加精确 `env_vars` 白名单：
  `ARK_API_KEY`、`VOLCENGINE_API_KEY`、`API_KEY_DOUBAO_CODING`、
  `OPENAI_API_KEY_DOUBAO`；
- manifest 不含静态 `env`，不保存任何凭据值；
- artifact 测试、release smoke、隔离插件验收和公共 npm 消费者验收精确验证
  白名单及静态值禁令；
- 隔离验收和公共 npm 验收按 manifest 白名单构造 MCP 环境，不再把完整父环境
  直接传给测试 transport；
- package、lock、runtime 和插件 manifest 统一升级到 `0.1.1-beta.1`；
- 不改变逻辑 LLM 注册表、真实模型、provider/route、凭据优先级、工具 schema、
  子进程最小环境、结果 validator 或历史能力 evidence。

## TDD 与当前证据

- artifact 测试先因 manifest 缺少 `env_vars` 失败，最小 manifest 与验收实现后
  8/8 转绿；
- 版本、CLI、artifact、资格 preflight 聚焦矩阵：42/42 passed；
- 隔离官方插件生命周期生成报告通过，随后 `--check-report` 只读复核通过；
- 缓存副本从 manifest 解析 `cwd: "."`，并只通过声明白名单收到凭据哨兵；
- 仓库和报告只保存变量名与布尔结论，不保存凭据值；
- 类型检查：passed；
- 首轮并行全库：52 个文件通过，资格 ledger 单项在 30 秒超时；其余
  890 passed / 1 skipped，没有断言失败；
- 超时用例单独复跑 7.96 秒通过；随后单 worker 全库：
  53 files / 891 passed / 1 skipped / 0 failed。没有修改生产代码或放宽时限；
- 能力索引：8/8 passed，legacy 受限入口 1；
- release smoke：passed；
- 生产依赖审计：0 vulnerabilities；
- npm pack dry-run：228 files / packed 598,812 bytes / unpacked
  3,037,677 bytes，未保留 `.tgz`；
- `git diff --check`：passed；
- 发布前 Kimi K3 窄范围 diff 外审在 300 秒超时且无结论，不计 PASS、未重试。
  超时后只清理了与调用时间精确对应的单个 owned Kimi 子进程，用户原有 Kimi
  桌面进程未终止，Pi 残留为 0；
- PR #2 merge commit `6d50a73` 已进入 `next`；
- CI run `30531374989` 的 Node 20/22/24 全绿；
- release run `30531642511` 通过单 worker 全量、release smoke、npm OIDC、
  registry/`next` 校验和 GitHub prerelease 创建；
- npm dist-tags：`next=0.1.1-beta.1`、`latest=0.1.0`；
- 公共 registry 精确版本验收通过 CLI、doctor、直接 MCP、官方临时插件生命周期、
  缓存副本 manifest `cwd` / `env_vars`、8/8 能力索引、目标进程 0/0/0 和资格
  锁 absent；真实模型调用 0，见
  [公共 npm 隔离验收](0.1.1-beta.1-npm-acceptance.md)；
- 活动插件首次官方 remove 在删除 beta.0 缓存时因文件占用失败；MCP 注册已移除，
  但 plugin list 仍显示 beta.0 installed。只读检查发现当前 App 宿主下存在多项
  命令行精确匹配该插件 runtime 的闲置 MCP 进程，最高观测 24 项；
- Codex 只终止这些插件 MCP 进程树，未终止 App、Kimi 桌面或旧工具进程。剩余
  精确目标为 0 后，官方 remove 成功显示 not installed，随后官方 add 安装
  0.1.1-beta.1；
- 官方 CLI 最终显示插件 installed/enabled 0.1.1-beta.1，`cwd` 解析到 beta.1
  缓存，四项 `env_vars` 均以 `*****` 脱敏显示；旧 `codex_cc_tools` 仍
  enabled；
- 修复版真实宿主门禁尚待完整 App 重启后执行。

## 公共验收前的取消残留

第一次公共 npm 验收在模型调用前首错停止，因为发布前 Kimi 外审的服务端工作仍在
活动 beta.0 MCP 中运行。外层工具调用已经超时，但 MCP 随后启动了新的 `kimi acp`
子进程；只杀该子进程会被同一 MCP 再次拉起。Codex 最终只终止了命令行精确匹配
插件 runtime 且拥有该 ACP 子进程的 MCP 进程树，保留 Kimi 桌面主进程、Codex
App 和旧 `codex_cc_tools`。5 秒复核为 0 后，公共验收通过。

这是一条真实取消门禁失败证据，不是 beta.1 的 `env_vars` 修复。稳定发布前必须在
隔离工作区验证：外层取消/超时会传播到 MCP 服务端，且 Kimi/Pi 子进程不重生。

## 真实宿主基线

- 完整进程重启后的验收任务：
  `019fb22c-9206-7352-aed1-bc47fafce56b`；
- 工具发现：新插件两项与旧插件两项共存；
- Kimi K3 review：单次调用完成，实际模型 `kimi-code/k3`，正确指出除以空数组长度
  会产生 `NaN`，工作区哈希与 Git 状态不变，结束后 Kimi 进程归零；
- Ark Coding Plan review：单次调用在启动 Pi 前因 MCP 缺少凭据失败，没有 retry、
  fallback、文件变化或残留 Pi 进程；
- 旧 `cc_review` 的 Ark Coding Plan 外审遇到周额度 429；新 Kimi 外审在 300 秒
  超时，二者均无结论、均不计 PASS，也没有同形重试。

## 发布停止条件

若官方升级或修复版真实宿主 Pi/delegate/取消清理任一失败，则保留 `0.1.0` 为 npm
`latest`，不得发布 stable，不得恢复开发期直连、手工修改活动配置、移除旧
`codex_cc_tools` 或修改 Claude Code。
