# codex-agent-tools 0.1.0 发布审阅

日期：2026-07-30

状态：稳定候选确定性门禁与独立复审已通过；尚未发布。

## 发布目标

`0.1.0` 只晋升已经验证的 `v0.1.0-beta.1`，不引入产品行为、逻辑 LLM
注册表、模型路由、凭据选择或能力 evidence 变更。稳定 tag 必须位于 `main`，
并由 `.github/workflows/release.yml` 通过 npm Trusted Publishing 发布到
`latest`。

## 已取得的候选证据

- `v0.1.0-beta.1` 所在提交为 `31e61cb`；
- Node 20 / 22 / 24 CI run `30507055208` 全绿；
- release run `30507178009` 通过真实 npm OIDC 发布、provenance、registry
  与 `next` dist-tag 复核，并创建 GitHub prerelease；
- 公共 registry 精确安装 `codex-agent-tools@0.1.0-beta.1` 后，CLI、doctor、
  stdio MCP、官方插件 add/list/remove、缓存副本 MCP 与 8/8 能力索引均通过；
  目标进程为 0/0/0，资格锁 absent，真实模型调用为 0；
- beta 验收的完整脱敏记录见
  [0.1.0-beta.1 公共 npm 隔离验收](0.1.0-beta.1-npm-acceptance.md)。

## 稳定候选范围

- package、runtime、插件 manifest 与当前版本合同统一为 `0.1.0`；
- README、运维说明、发布清单和真实宿主审阅材料同步稳定候选状态；
- 新增 `.release-validation/v0.1.0.md`，只在稳定候选的完整确定性矩阵与独立
  发布复审通过后写入全部精确 `pass` marker；
- 不运行真实模型，不访问或修改活动 `~/.codex/config.toml`，不安装或替换
  活动插件，不移除旧 `codex_cc_tools`，不修改 Claude Code。

## 稳定候选验证

- 版本 TDD：稳定版本期望先红，更新 package/runtime/plugin 四个版本载体后，
  4 files / 39 tests passed；
- 全量测试：53 files / 890 passed / 1 platform-conditional skipped /
  0 failed；
- 类型检查：passed；
- package/runtime/plugin 版本一致性：passed，均为 `0.1.0`；
- 能力索引：8/8 passed，legacy 受限入口 1；
- release smoke：passed；
- 生产依赖审计：0 vulnerabilities；
- npm pack dry-run：228 files；
- `cc_review` 稳定发布差异复审：PASS，无 P0/P1。三项非阻断文档建议均已采纳：
  修正 stable 分支标识、刷新当前测试数字、逐字列出 validation marker。

稳定 tag 指向的提交必须包含以下精确 marker：

```text
RC: v0.1.0-beta.1
Doctor: pass
Local-Npm-Smoke: pass
MCP-Smoke: pass
Plugin-Isolated: pass
Capability-Index: pass
Release-Review: pass
```

对应证据已经写入 [v0.1.0 Stable Release Validation](../../.release-validation/v0.1.0.md)。

## 失败与重试记录

- 第一轮全量单 worker 中，`test/qualification/verifier.test.ts` 的两个临时 Git
  仓库夹具在全套负载下触发固定 30 秒测试超时；其余 888 passed / 1 skipped。
  两项聚焦复跑分别在 1.8 秒和 1.5 秒通过，完整 verifier 文件 94/94 通过；
  随后的第二轮全库复跑 53 files / 890 passed / 1 skipped / 0 failed。该事件
  只记为负载相关夹具超时，没有修改生产 verifier 或放宽测试时限。

## 尚待完成

- `main` CI、`v0.1.0` tag-triggered OIDC 发布、npm `latest` 与 GitHub Release；
- 从公共 registry 精确安装 `codex-agent-tools@0.1.0` 的发布后隔离复验。

已发布版本不得覆盖或复用；任一门禁失败都必须先修复并重新验证，再创建稳定 tag。

预期发布标签：

```text
v0.1.0
```
