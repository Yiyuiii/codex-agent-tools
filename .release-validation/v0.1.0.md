# codex-agent-tools v0.1.0 Stable Release Validation

Checked on: 2026-07-30

RC: v0.1.0-beta.1

Doctor: pass
Local-Npm-Smoke: pass
MCP-Smoke: pass
Plugin-Isolated: pass
Capability-Index: pass
Release-Review: pass

## Evidence

- `v0.1.0-beta.1` was published by GitHub Actions OIDC in release run
  `30507178009`; npm provenance, registry propagation, `next` dist-tag and
  GitHub prerelease creation passed.
- The exact public package `codex-agent-tools@0.1.0-beta.1` passed isolated
  CLI, doctor, stdio MCP, official plugin add/list/remove, cached-plugin MCP,
  process cleanup and qualification-lock checks. Product real-model calls: 0.
- The stable candidate passed 53 test files / 890 tests with 1
  platform-conditional skip, typecheck, build, 8/8 capability verification,
  release smoke and a production dependency audit with 0 vulnerabilities.
- Stable `npm pack --dry-run --json` reported version `0.1.0`, 228 files,
  597,400 packed bytes and 3,033,698 unpacked bytes.
- Independent `cc_review` returned PASS with no P0/P1 issue. All three
  non-blocking documentation suggestions were applied before this record.

## Boundaries

- `0.1.0` changes release version metadata and current-version documentation
  and tests only; product behavior, logical LLM registry, routes, credentials
  and immutable capability evidence are unchanged from the accepted RC.
- No product real model, active Codex configuration or plugin, Claude Code, or
  legacy `codex_cc_tools` installation was invoked or modified.
- This file authorizes the deterministic stable release workflow. It does not
  claim that the public `0.1.0` package has already been published or that the
  plugin has been installed into the active Codex App.
